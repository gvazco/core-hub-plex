---
import CoreLayout from '@/layouts/CoreLayout.astro';
import GalleryCategories from '@/components/ui/Taxonomies/GalleryCategories.astro';
import GalleryTags from '@/components/ui/Taxonomies/GalleryTags.astro';
import GalleryPosts from '@/components/galleries/GalleryPosts.astro';
import { DirectusGalleriesPageResponseSchema } from '@/types';
import AwesomeSliderGalleryWrapper from '@/components/galleries/AwesomeSliderGalleryWrapper.astro';
const newUrl = `${import.meta.env.API_ITEMS}/pages?filter[slug][_eq]=galerias`;
const newResponse = await fetch(newUrl);
const newJson = await newResponse.json();
const newData = DirectusGalleriesPageResponseSchema.parse(newJson);
const { data: galleriesPage } = newData;
const publicAssetsUrl = import.meta.env.PUBLIC_ASSETS;
---

<CoreLayout
  title={galleriesPage[0]?.title || 'Galerías'}
  subtitle={galleriesPage[0]?.subtitle ||
    'Explora las galerías de nuestros autores y descubre su trabajo'}
  bgImage={`${publicAssetsUrl}/${galleriesPage[0]?.cover_image}`}
>
  <AwesomeSliderGalleryWrapper />
  <GalleryCategories />
  <GalleryTags />
  <GalleryPosts />
</CoreLayout>
