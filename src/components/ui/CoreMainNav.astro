---
const navigation = [
  { name: 'Blog', href: '/blog' },
  { name: 'Galerías', href: '/galerias' },
  { name: 'Colaboradores', href: '/' },
];

const currentPath = Astro.url.pathname;
const isActive = (href: string) => {
  if (href === '/') return currentPath === '/';
  if (href === '/blog') return currentPath === '/blog';
  return currentPath === href || currentPath.startsWith(`${href}/`);
};
---

<nav class="hidden md:flex gap-8 font-mono font-bold text-lg">
  {
    navigation.map((item) => (
      <a
        href={item.href}
        class={`${isActive(item.href) ? 'text-neon-yellow' : 'text-white hover:text-neon-yellow'} uppercase text-lg font-bold`}
      >
        {item.name}
      </a>
    ))
  }
</nav>
