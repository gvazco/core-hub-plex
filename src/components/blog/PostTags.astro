---
interface Props {
  name: string;
  slug: string;
}

const { name, slug } = Astro.props;
---

<a
  href={`/blog/etiqueta/${slug}`}
  class="max-w-xs inline-block py-1 px-5 mx-3 text-sm text-white bg-dark neo-border-cyan p-5"
>
  {name}
</a>
