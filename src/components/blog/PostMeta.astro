---
import { formatDate } from '@/helpers';

interface Props {
  date: string;
}

const { date } = Astro.props;
---

<p class="text-sm">
  Escrito el: <span class="text-neon-yellow">{formatDate(date)}</span>
</p>
